public enum MessageType {
    // Connection
    CONNECTION_SUCCESS, CONNECT, DISCONNECT, USERNAME_VALIDATION, USERNAME_ACCEPTED, USERNAME_TAKEN,

    // User List
    USER_LIST,

    // Chat (not used yet)
    CHAT,

    // Notifications
    ERROR
}